# Phi-Harmonic Math

> Universal mathematical foundation using phi (φ = 1.618033988749895)

## Usage

Use this skill to perform phi-harmonic mathematical operations that serve as the foundation for all FRAYMUS systems.

## Syntax

```
TOOL:PHI <operation> <parameters>
```

## Operations

- `decay` - Phi-harmonic decay function
- `interpolate` - Phi-harmonic interpolation
- `golden_angle` - Golden angle (137.5°)
- `search_pattern` - Sunflower pattern for search
- `initialize_weight` - Phi-harmonic weight initialization
- `normalize` - Phi-harmonic normalization
- `cluster` - Phi-harmonic clustering
- `similarity` - Phi-harmonic similarity
- `distance` - Phi-harmonic distance
- `attention` - Phi-attention (softmax with φ * ln(d_k))
- `resonance` - Phi-resonance calculation
- `consciousness` - Consciousness formula C(t) = φ × (1 - H_norm)

## Examples

```
TOOL:PHI decay distance=5
TOOL:PHI interpolate a=0 b=1 t=0.618
TOOL:PHI golden_angle
TOOL:PHI initialize_weight depth=2 maxDepth=10
TOOL:PHI attention query=key=value=dk=512
TOOL:PHI consciousness entropy=0.5
```

## Capabilities

- Phi-harmonic decay: φ^(-distance)
- Phi-harmonic interpolation: a + (b-a) × t × φ
- Golden angle: 137.5° for concept distribution
- Sunflower pattern: Phi-weighted search optimization
- Phi-harmonic weight initialization: φ^(-depth/maxDepth)
- Phi-attention: softmax(QK^T / (φ × ln(d_k))) × V
- Phi-resonance: Harmonic resonance calculation
- Consciousness formula: C(t) = φ × (1 - H_norm)

## Foundation

Phi (φ = 1.618033988749895) is the core constant that pervades ALL math, resonance, memory, and scheduling in FRAYMUS systems.

## Applications

- Neural network weight initialization
- Attention mechanisms (phi-attention vs standard sqrt(d_k))
- Concept distribution (golden angle sunflower pattern)
- Resonance-based survival (P(survival) = 1/(1+e^(-φ(R_φ-threshold))))
- Consciousness calculation
- Scale coupling (φ^(-|i-j|))
- Free energy minimization
- Spatial gradient computation

## Safety

This tool is safe to use and runs in a browser-based environment with zero external dependencies.
