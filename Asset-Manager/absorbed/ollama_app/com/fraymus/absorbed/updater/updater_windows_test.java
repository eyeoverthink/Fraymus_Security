package com.fraymus.absorbed.updater;

import java.util.*;
import java.io.*;

public class updater_windows_test {
        "log/slog";
        "testing";
        );

    public static void TestIsInstallerRunning(*testing.T t) {
        slog.SetLogLoggerLevel(slog.LevelDebug);
        Installer = "go.exe";
        if !isInstallerRunning() {
        t.Fatal("not running");
    }
    }
}
